<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Anagram Anabel</title>
  </head>
  <body>
    <h2>Anagram X Anabel</h2>
    <form class="" action="/" method="post">
       <input type="text" name="word" value="">
       <button type="submit" > Anagram it! </button>
    </form>
    <b><%= anagrams %></b>
   <ul>
     <% for(var i=0; i < anagrams.length; i++){   %>
       <li><%= anagrams[i].words %></li>
      <% } %>
    </ul>
  </body>
</html>
